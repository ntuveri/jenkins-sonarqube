pipeline {
    agent any

    stages {
        stage('Hello world') {
            steps {
                script {
                    echo "Build based on Jenkinsfile"
                }
            }
        }
    }
}
